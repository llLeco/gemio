{
  "version": 2,
  "builds": [
    { "src": "backend/src/main.ts", "use": "@vercel/node" },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/gemio" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/src/main.ts" },
    { "src": "/(.*)", "dest": "/dist/gemio/$1" }
  ]
}
